import { InquiryForm } from './InquiryForm';
import { SITE } from '@/lib/site';

export function Contact() {
  return (
    <section id="contact" className="py-24 md:py-32">
      <div className="container-page">
        <div className="grid grid-cols-1 gap-16 md:grid-cols-2">
          <div>
            <p className="label-uppercase mb-6">Contact</p>
            <h2 className="font-display text-h1 text-primary mb-8">
              {/* Replace with specific CTA */}
              Start a project.
            </h2>
            <p className="text-body text-text-muted max-w-sm mb-8">
              {/* Be specific — turnaround, what to include in brief */}
              Tell us what you're working on. We'll get back within 48 hours.
            </p>
            <a
              href={`mailto:${SITE.email}`}
              className="font-mono text-mono text-accent hover:underline inline-block py-2"
            >
              {SITE.email}
            </a>
          </div>

          <div>
            <InquiryForm />
          </div>
        </div>
      </div>
    </section>
  );
}

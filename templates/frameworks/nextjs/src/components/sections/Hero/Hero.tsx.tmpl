'use client';

import { useReducedMotion } from '@/hooks/useReducedMotion';
import { Button } from '@/components/ui/Button';
import { SITE } from '@/lib/site';

export function Hero() {
  const reduced = useReducedMotion();

  return (
    <section className="relative min-h-dvh flex items-center overflow-hidden">
      {/* Background media */}
      <div className="absolute inset-0 z-0">
        {!reduced && (
          <video
            autoPlay
            muted
            loop
            playsInline
            poster="/images/hero-poster.jpg"
            className="w-full h-full object-cover opacity-30"
          >
            <source src="/videos/hero-loop.webm" type="video/webm" />
            <source src="/videos/hero-loop.mp4" type="video/mp4" />
          </video>
        )}
        <div className="absolute inset-0 bg-gradient-to-b from-bg/20 via-transparent to-bg" />
      </div>

      {/* Content */}
      <div className="relative z-10 container-page pt-32 pb-24">
        <p className="label-uppercase mb-8">{SITE.name} — {SITE.city}</p>

        <h1 className="font-display text-display text-primary max-w-3xl mb-8 leading-none">
          {/* Replace with your actual headline */}
          {{TAGLINE}}
        </h1>

        <p className="text-body-lg text-text-muted max-w-xl mb-12">
          {/* Replace with specific description — name formats, specs, location */}
          [Your specific description here. Avoid vague adjectives. Name what you do and where.]
        </p>

        <div className="flex flex-wrap gap-4">
          <Button href="#contact" variant="primary" size="lg">
            Start a project
          </Button>
          <Button href="#works" variant="secondary" size="lg">
            See the work
          </Button>
        </div>
      </div>
    </section>
  );
}

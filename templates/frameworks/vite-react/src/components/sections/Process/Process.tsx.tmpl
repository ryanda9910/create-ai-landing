import { PROCESS_STEPS } from '@/lib/process';
import { cn } from '@/lib/utils';

export function Process() {
  return (
    <section id="process" className="py-24 md:py-32 bg-surface">
      <div className="container-page">
        <div className="mb-16 md:mb-24">
          <p className="label-uppercase mb-4">Process</p>
          <h2 className="font-display text-h1 text-primary max-w-xl">
            Transparent from day one.
          </h2>
        </div>

        <div className="flex flex-col gap-0">
          {PROCESS_STEPS.map((step, i) => {
            const isEven = i % 2 === 0;
            return (
              <div
                key={step.number}
                className="border-t border-border py-12 md:py-16 grid grid-cols-1 md:grid-cols-[1fr_auto_1fr] gap-8 md:gap-16 items-start"
              >
                {/* Left column */}
                <div className={cn(isEven ? 'md:col-start-1' : 'md:col-start-3 md:row-start-1')}>
                  {!isEven && (
                    <div>
                      <p className="text-body text-text-muted leading-relaxed">{step.description}</p>
                      <p className="font-mono text-mono text-accent mt-4">→ {step.detail}</p>
                    </div>
                  )}
                  {isEven && (
                    <div>
                      <span className="font-mono text-mono text-text-muted">{step.number}</span>
                      <h3 className="font-display text-h2 text-primary mt-2">{step.title}</h3>
                      <p className="label-uppercase text-accent mt-4">{step.duration}</p>
                    </div>
                  )}
                </div>

                {/* Center divider */}
                <div className="hidden md:flex flex-col items-center pt-2">
                  <div className="w-px h-full min-h-[60px] bg-border" />
                </div>

                {/* Right column */}
                <div className={cn(isEven ? 'md:col-start-3' : 'md:col-start-1 md:row-start-1')}>
                  {isEven && (
                    <div>
                      <p className="text-body text-text-muted leading-relaxed">{step.description}</p>
                      <p className="font-mono text-mono text-accent mt-4">→ {step.detail}</p>
                    </div>
                  )}
                  {!isEven && (
                    <div>
                      <span className="font-mono text-mono text-text-muted">{step.number}</span>
                      <h3 className="font-display text-h2 text-primary mt-2">{step.title}</h3>
                      <p className="label-uppercase text-accent mt-4">{step.duration}</p>
                    </div>
                  )}
                </div>
              </div>
            );
          })}
        </div>
      </div>
    </section>
  );
}

import { SERVICES } from '@/lib/services';
import { cn } from '@/lib/utils';

export function Services() {
  return (
    <section id="services" className="py-24 md:py-32">
      <div className="container-page">
        <div className="mb-16">
          <p className="label-uppercase mb-4">Services</p>
          <h2 className="font-display text-h1 text-primary max-w-xl">
            {/* Replace with specific headline */}
            What we do.
          </h2>
        </div>

        <div className="grid grid-cols-1 md:grid-cols-3 gap-px bg-border">
          {SERVICES.map((service) => (
            <div
              key={service.id}
              className={cn(
                'relative bg-bg p-10 flex flex-col gap-8 transition-all duration-300',
                'group hover:bg-surface',
                'before:absolute before:left-0 before:top-0 before:h-full before:w-px',
                'before:bg-accent before:scale-y-0 before:origin-top',
                'before:transition-transform before:duration-500',
                'hover:before:scale-y-100',
              )}
            >
              <p className="font-display text-h3 text-primary">{service.title}</p>
              <p className="text-body text-accent">{service.tagline}</p>
              <p className="text-body text-text-muted">{service.description}</p>
              <ul className="flex flex-col gap-2 mt-auto">
                {service.deliverables.map((d) => (
                  <li key={d} className="font-mono text-mono text-text-muted flex gap-2">
                    <span className="text-accent">→</span>
                    <span>{d}</span>
                  </li>
                ))}
              </ul>
            </div>
          ))}
        </div>
      </div>
    </section>
  );
}
